import React from 'react';
import OrderList from('../components/orderList');

const Orders = () => {
    return (
        <div>
            <h2>Orders</h2>
            <OrderList />
        </div>
    );
};

export default Orders;